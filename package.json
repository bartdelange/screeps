{
  "name": "screeps-monorepo",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "apps/screeps-bot",
    "apps/screeps-metric-processor"
  ],
  "scripts": {
    "nx": "nx",
    "build": "nx run-many -t build",
    "dev": "nx run-many -t dev --parallel",
    "graph": "nx graph"
  },
  "devDependencies": {
    "@nx/js": "^20.0.0",
    "@nx/rollup": "^20.0.0",
    "nx": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
